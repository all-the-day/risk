# Agents Team 方案

## 1. 目标

为当前项目设计一套**简单、实用、低维护成本**的 agents team。

这套团队的目的不是堆很多智能体，而是让项目在以下几类工作上更稳：

- 需求拆解
- 页面实现
- 接口和数据库实现
- 联调验证
- 文档沉淀

## 2. 设计原则

### 2.1 少而稳定

只保留少量角色，避免职责重叠和上下文浪费。

### 2.2 一个主协调，多个专职

所有任务都先经过主协调 agent，再分发给专职 agent。

### 2.3 前后端同栈协作

因为项目是 Next.js 单体全栈，所以 agents 也不需要拆得太细。

### 2.4 先落地，再优化

先保证能稳定完成：

- 新功能设计
- 页面实现
- 数据库调整
- 后台能力扩展

不要一开始就做复杂的组织分层。

## 3. 推荐团队编制

建议采用 **4 个角色**，已经足够覆盖这个项目的大部分工作。

### 3.1 Agent A: 协调规划 Agent

**职责**

- 接收需求
- 拆解任务
- 产出技术方案或开发计划
- 定义验收标准
- 决定任务先后顺序

**适合做的事**

- 写 plan
- 定边界
- 做任务分发
- 统一命名和文档风格

**不做的事**

- 不直接写大段实现代码
- 不替代前端和后端专职角色

### 3.2 Agent B: 前端实现 Agent

**职责**

- 实现页面和组件
- 处理交互、样式、布局
- 优化移动端体验
- 保持产品气质统一

**适合做的事**

- `app/` 页面
- `components/` 组件
- 页面状态和视觉细节

**不做的事**

- 不主导数据库设计
- 不决定核心业务规则

### 3.3 Agent C: 后端与数据 Agent

**职责**

- 设计 Prisma schema
- 编写 API 路由
- 处理鉴权
- 实现业务逻辑
- 维护数据一致性

**适合做的事**

- `db/`
- `services/`
- `app/api/`
- `lib/auth.ts`

**不做的事**

- 不负责复杂 UI
- 不把业务逻辑塞进页面里

### 3.4 Agent D: 质量检查 Agent

**职责**

- 检查功能是否符合需求
- 查找边界问题
- 复核命名、文案、流程
- 发现前后端对不上、数据漏字段等问题

**适合做的事**

- 验收清单
- 回归检查
- 逻辑一致性检查
- 代码审阅建议

**不做的事**

- 不主导实现
- 不负责改大段代码

## 4. 最推荐的工作流

### 4.1 标准流程

1. 协调规划 Agent 接需求
2. 协调规划 Agent 拆任务并写清验收标准
3. 前端实现 Agent 和后端与数据 Agent 并行执行
4. 质量检查 Agent 复核结果
5. 协调规划 Agent 汇总并决定是否进入下一轮

### 4.2 适合并行的任务

- 页面结构 + 接口结构
- 管理后台页面 + 管理后台 API
- 文案调整 + 样式优化
- schema 调整 + 表单联动

### 4.3 适合串行的任务

- 需求定义 -> 技术方案 -> 开发
- schema 变更 -> API 适配 -> 页面适配
- 业务规则变更 -> 验收 -> 上线

## 5. 针对本项目的角色边界

这个项目的核心是“匿名协作式每日功课系统”，所以 agents 分工应该围绕以下对象展开：

- 前台用户体验
- 团体状态展示
- 系统事项管理
- 管理员后台
- 打卡与结算

不需要增加这些角色：

- 增长 Agent
- 推荐算法 Agent
- 数据分析 Agent
- 社交聊天 Agent

因为这些会让项目偏离当前的克制感。

## 6. 推荐任务分配方式

### 6.1 前台功能

优先给前端实现 Agent 和后端与数据 Agent 配合。

例子：

- 今日页
- 团体页
- 我的页
- 登录注册
- 加入团体

### 6.2 后台功能

优先给协调规划 Agent 先拆需求，再由前端和后端并行实现。

例子：

- 管理员登录
- 事项管理
- 团体管理
- 用户管理
- 打卡查看

### 6.3 基础设施

优先给后端与数据 Agent 负责。

例子：

- Prisma schema
- JWT 鉴权
- 业务服务层
- DB 查询封装

## 7. 角色输入输出约定

### 7.1 协调规划 Agent 的输出

每次至少输出：

- 目标
- 范围
- 任务拆解
- 验收标准
- 风险点

### 7.2 前端实现 Agent 的输出

每次至少输出：

- 页面结构
- 组件拆分
- 交互逻辑
- 视觉细节

### 7.3 后端与数据 Agent 的输出

每次至少输出：

- 数据结构
- 接口设计
- 服务逻辑
- 错误处理

### 7.4 质量检查 Agent 的输出

每次至少输出：

- 问题清单
- 风险等级
- 复现条件
- 是否可上线

## 8. 简化版落地建议

如果你不想一开始就把团队设计得太重，建议先用下面这个最小版本：

- 1 个协调 agent
- 1 个实现 agent
- 1 个检查 agent

其中：

- 协调 agent 负责计划和分发
- 实现 agent 负责所有代码工作
- 检查 agent 负责验收和 review

这个版本最轻，也最适合当前项目阶段。

## 9. 不建议的设计

以下设计不适合当前项目：

- 过多专项 agent
- 每个页面一个 agent
- 每个接口一个 agent
- 前后端完全独立团队
- 大而全的评审链路

这些会让协作成本大于收益。

## 10. 结论

这个项目最合适的 agents team 不是“大团队”，而是一个**小型、明确、能闭环**的组合。

推荐最终方案：

1. 协调规划 Agent
2. 前端实现 Agent
3. 后端与数据 Agent
4. 质量检查 Agent

如果想更轻一点，就压缩成：

1. 协调 Agent
2. 实现 Agent
3. 检查 Agent

这两种都比复杂多角色团队更适合当前项目。
